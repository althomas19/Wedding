# Wedding Website

AT and GC Wedding website!

## Development

Requires Ruby 2.5+ and Bundler.

```bash
bundle install
bundle exec jekyll serve
```

Site runs at `http://localhost:4000`

## Deployment
Github actions configured to deploy to github pages